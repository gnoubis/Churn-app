apiVersion: v1
kind: Service
metadata:
  name: churn-model-service
spec:
  type: ClusterIP
  selector:
    app: churn-model-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
